{
    "name": "@nexus/menubar-app",
    "version": "0.1.0",
    "private": true,
    "description": "Nexus menu bar MVP for inbox summary and quick pull request actions",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsc -p ./tsconfig.json",
        "test": "vitest run"
    },
    "devDependencies": {
        "@types/node": "^22.0.0",
        "typescript": "^5.7.0",
        "vitest": "^2.1.0"
    },
    "engines": {
        "node": ">=20.0.0"
    }
}
